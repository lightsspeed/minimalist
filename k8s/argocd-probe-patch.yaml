spec:
  template:
    spec:
      containers:
      - name: argocd-server
        livenessProbe:
          httpGet:
            scheme: HTTP
        readinessProbe:
          httpGet:
            scheme: HTTP
