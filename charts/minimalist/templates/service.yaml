apiVersion: v1
kind: Service
metadata:
  name: {{ include "minimalist.fullname" . }}
  labels:
    {{- include "minimalist.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minimalist.selectorLabels" . | nindent 4 }}
