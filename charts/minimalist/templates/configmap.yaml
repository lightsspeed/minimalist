apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "minimalist.fullname" . }}-config
  labels:
    {{- include "minimalist.labels" . | nindent 4 }}
data:
  VITE_SUPABASE_URL: {{ .Values.env.supabaseUrl | quote }}
  VITE_SUPABASE_PROJECT_ID: {{ .Values.env.supabaseProjectId | quote }}
